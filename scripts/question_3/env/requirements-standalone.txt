# ============================================================
# Standalone (pip-only) requirements for Question 3
# Compatible with legacy tf.keras.optimizers.legacy
# ============================================================

# --- Core numerical stack ---
numpy>=1.23,<2.0
scipy>=1.9
pandas>=1.5
matplotlib>=3.6
tqdm
joblib
scikit-learn
statsmodels

# --- TensorFlow stack (LOCKED) ---
tensorflow==2.15.*
tensorflow-probability==0.23.*

# --- Keras compatibility (CRITICAL) ---
keras<3
tf-keras

# --- Utilities ---
absl-py
protobuf<5
typing-extensions
