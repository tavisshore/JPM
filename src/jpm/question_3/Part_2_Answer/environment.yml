name: q3p2

channels:
  - conda-forge

dependencies:
  # Python
  - python=3.10

  # Core scientific stack
  - numpy>=1.23,<2.0
  - scipy>=1.9
  - pandas>=1.5
  - tqdm

  # TensorFlow (Apple Silicon compatible)
  - tensorflow=2.16.*
  - tensorflow-probability=0.23.*

  # Choice modeling
  - pip
  - pip:
      - choice-learn==1.3.1
